/ frozen_string_literal: true

- breadcrumb :item, @article, @article_presenter

= render Elements::Header::Component.new header: header(@article_presenter),
  main_header: true,
  item: @article,
  action_buttons: [edit_button(@article_presenter),
    destroy_button(@article_presenter)]

= render Elements::Card::Component.new item: @article,
  item_presenter: @article_presenter,
  details: [:id,
    :title,
    :content]
